DB_CONNECTION_STRING="postgres://postgres:admin123@postgres:5432/nomadcrew"

PORT=8080

REDIS_ADDRESS="redis:6379"
REDIS_PASSWORD="redispass"
REDIS_DB="0" 
SUPABASE_PROJECT_ID=REPLACE

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin123
DB_NAME=nomadcrew
DB_SSL_MODE=disable

# Redis
REDIS_ADDRESS=localhost:6379
REDIS_PASSWORD=redispass
REDIS_DB=0

# Server Configuration
SERVER_ENVIRONMENT=REPLACE
ALLOWED_ORIGINS=*
FRONTEND_URL=REPLACE

# Email Configuration
EMAIL_FROM_ADDRESS=REPLACE
EMAIL_FROM_NAME=REPLACE
RESEND_API_KEY=REPLACE

# AWS Configuration
AWS_REGION=REPLACE
AWS_SECRETS_PATH=REPLACE

# Secrets
JWT_SECRET_KEY=REPLACE
GEOAPIFY_KEY=REPLACE
PEXELS_API_KEY=REPLACE
# Supabase API Keys (new naming: publishable replaces anon, secret replaces service_role)
SUPABASE_ANON_KEY=REPLACE
SUPABASE_SERVICE_KEY=REPLACE
SUPABASE_URL="https://eihszqnmmgbrcxtymskn.supabase.co"
# For JWT verification, use JWKS endpoint: https://eihszqnmmgbrcxtymskn.supabase.co/auth/v1/.well-known/jwks.json
SUPABASE_JWT_SECRET=REPLACE
LOG_LEVEL=debug

# Rate Limiting Configuration
# Maximum requests per minute for auth endpoints (login, register, password reset)
RATE_LIMIT_AUTH_REQUESTS_PER_MINUTE=10
# Time window in seconds for rate limiting
RATE_LIMIT_WINDOW_SECONDS=60
